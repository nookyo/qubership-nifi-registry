branches-template:
  main: "{{actor}},.{{timestamp}}"
  feature*: "{{ref}}.{{timestamp}}"
  release*: "{{name}}.{{timestamp}}"
  develop: "SNAPSHOT-{{timestamp}}"
  tag: "{{ref}}"
dist-tags:
  main: "latest"
  release*: "RELEASE"
  develop: "dev"
  tag: "tag"

