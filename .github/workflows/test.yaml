name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
        - name: Checkout
          uses: actions/checkout@v4

        - name: Github Metadata action
          uses: nookyo/qubership-workflow-hub/actions/github-metadata@main
          with:
            template: 'SNAPSHOT-{{combine}}-v{{major}}.{{minor}}.{{patch}}-{{tag}}-{{ sha }}'
            branch-template: '{
                                "main":"{{ref.name}}.{{combine}}",
                                "release":"v{{major}}.{{minor}}.{{patch}}",
                                "dev":"SNAPSHOT-{{combine}}"
                              }'


                # main: {{ref_name}}.{{sha}}
                # release: v{{major}}.{{minor}}.{{patch}}
                # develop: SNAPSHOT-{{combine}}


